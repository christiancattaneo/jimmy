function m(t,e,n){let i=`${t}:${e}:${n}`,r=0;for(let a=0;a<i.length;a++)r=r*31+i.charCodeAt(a)|0;return`JIMMY-${(r>>>0).toString(16).padStart(8,"0").toUpperCase()}`}function f(t){let e=t.split(/[/\\]/);return e[e.length-1]||t}function h(t){let e="",n=0,i=!1,r=!1,a=null;for(;n<t.length;){let s=t[n],d=n+1<t.length?t[n+1]:"";if(i){s===`
`&&(i=!1,e+=s),n++;continue}if(r){if(s==="*"&&d==="/"){r=!1,n+=2;continue}n++;continue}if(a){e+=s,s===a&&(a=null),n++;continue}if(s==="-"&&d==="-"){i=!0,n+=2;continue}if(s==="/"&&d==="*"){r=!0,n+=2;continue}if(s==="'"||s==='"'){a=s,e+=s,n++;continue}e+=s,n++}return e}function y(t){let e=h(t),n=[],i="",r=1,a=1,s=null,d=null,g=!1;for(let c=0;c<e.length;c++){let l=e[c];if(i.trim().length===0&&l!==`
`&&(r=a),g){i+=l,l===`
`&&a++;let o=`$${d}$`;l==="$"&&e.startsWith(o,c)&&(i+=o.slice(1),c+=o.length-1,g=!1,d=null);continue}if(s){i+=l,l===`
`&&a++,l===s&&(s=null);continue}if(l==="$"){let o=e.slice(c).match(/^\$([a-zA-Z_]*)\$/);if(o){d=o[1]??"",g=!0,i+=o[0],c+=o[0].length-1;continue}}if(l==="'"||l==='"'){s=l,i+=l;continue}if(l===";"){let o=i.trim();o.length>0&&n.push({text:o,line:r}),i="";continue}i+=l,l===`
`&&a++}let u=i.trim();return u.length>0&&n.push({text:u,line:r}),n}var E=[{id:"migration.add-not-null-without-default",severity:"high",title:"ADD COLUMN ... NOT NULL without DEFAULT",description:"Adding a NOT NULL column without a default rewrites the whole table on Postgres < 11 and fails on tables with existing rows. Use a default or backfill in two steps.",test:t=>/alter\s+table\s+[^;]*add\s+column[^;]*not\s+null/i.test(t)&&!/default/i.test(t)},{id:"migration.non-concurrent-index",severity:"high",title:"CREATE INDEX without CONCURRENTLY",description:"CREATE INDEX takes an ACCESS EXCLUSIVE on writers. On a busy table this stalls every write until the build finishes. Use CREATE INDEX CONCURRENTLY (and split it into its own migration; CONCURRENTLY cannot run inside a transaction).",test:t=>/create\s+(unique\s+)?index/i.test(t)&&!/concurrently/i.test(t)&&!/temp(orary)?/i.test(t)},{id:"migration.type-rewrite",severity:"high",title:"ALTER COLUMN TYPE that may rewrite the table",description:"Changing a column's type usually rewrites every row. For long-running services, use a shadow column + backfill + swap.",test:t=>/alter\s+table\s+[^;]*alter\s+column\s+[^;]*type\s+/i.test(t)},{id:"migration.add-fk-without-not-valid",severity:"medium",title:"ADD FOREIGN KEY without NOT VALID",description:"Adding a foreign key validates every existing row under a SHARE ROW EXCLUSIVE lock, blocking writes to both tables for the duration. Add the constraint NOT VALID, then VALIDATE CONSTRAINT in a separate statement (validation takes a weaker lock).",test:t=>/alter\s+table\s+[^;]*add\s+(constraint\s+\S+\s+)?foreign\s+key/i.test(t)&&!/not\s+valid/i.test(t)},{id:"migration.add-check-without-not-valid",severity:"medium",title:"ADD CHECK constraint without NOT VALID",description:"Adding a CHECK constraint scans the whole table to validate under an ACCESS EXCLUSIVE lock. Add it NOT VALID, then VALIDATE CONSTRAINT separately.",test:t=>/alter\s+table\s+[^;]*add\s+(constraint\s+\S+\s+)?check\s*\(/i.test(t)&&!/not\s+valid/i.test(t)},{id:"migration.add-unique-constraint",severity:"high",title:"ADD UNIQUE constraint (builds an index under an exclusive lock)",description:"ALTER TABLE ... ADD CONSTRAINT ... UNIQUE builds the backing index while holding ACCESS EXCLUSIVE. Build a unique index CONCURRENTLY first, then ADD CONSTRAINT ... USING INDEX.",test:t=>/alter\s+table\s+[^;]*add\s+(constraint\s+\S+\s+)?unique\s*\(/i.test(t)&&!/using\s+index/i.test(t)},{id:"migration.volatile-default",severity:"medium",title:"ADD COLUMN with a volatile DEFAULT",description:"Adding a column whose default is a volatile function (random(), gen_random_uuid(), clock_timestamp()) forces a full table rewrite to materialize a distinct value per row, unlike a constant default which is metadata-only on modern Postgres. Backfill in batches instead.",test:t=>/alter\s+table\s+[^;]*add\s+column[^;]*default\s+[^;]*(random\s*\(|gen_random_uuid\s*\(|uuid_generate_v\d\s*\(|clock_timestamp\s*\()/i.test(t)},{id:"migration.partitioned-index",severity:"medium",title:"CREATE INDEX on a partitioned table",description:"Creating an index on a partitioned table is not supported with CONCURRENTLY. Create the index on each partition CONCURRENTLY, then attach, or create it ONLY on the parent and build children separately to avoid a long lock.",test:t=>/create\s+(unique\s+)?index\s+concurrently[^;]*\bon\s+only\b/i.test(t)},{id:"migration.fk-set-default",severity:"medium",title:"Foreign key with ON DELETE/UPDATE SET DEFAULT",description:"ON DELETE SET DEFAULT silently rewrites child rows to a default value when a parent is removed. If the default is NULL or a non-existent key it can violate the constraint or orphan data semantically. Prefer explicit CASCADE/RESTRICT and confirm the default row exists.",test:t=>/references\b[^;]*on\s+(delete|update)\s+set\s+default/i.test(t)||/foreign\s+key[^;]*on\s+(delete|update)\s+set\s+default/i.test(t)},{id:"migration.unsafe-restore",severity:"low",title:"Statement only safe at restore time",description:"ALTER TABLE ... DISABLE TRIGGER ALL and SET session_replication_role = replica disable integrity enforcement. They belong in a controlled restore, not a routine migration, where they can let bad data slip past FKs and triggers.",test:t=>/disable\s+trigger\s+all/i.test(t)||/set\s+session_replication_role\s*=\s*'?replica'?/i.test(t)},{id:"migration.drop-column",severity:"medium",title:"DROP COLUMN",description:"Dropping a column breaks any code that still references it. Even if you have updated all callers, an in-flight deploy may still be running the old code.",test:t=>/alter\s+table\s+[^;]*drop\s+column/i.test(t)},{id:"migration.drop-table",severity:"high",title:"DROP TABLE",description:"Dropping a table is destructive. Verify nothing reads from it (including replicas, queues, and external tooling) before merging.",test:t=>/^drop\s+table\b/i.test(t)},{id:"migration.rename-column",severity:"medium",title:"RENAME COLUMN",description:"Renames are not backwards compatible. Either deploy the rename in two phases (add new column, dual-write, switch reads, drop old) or expect old code to crash mid-deploy.",test:t=>/alter\s+table\s+[^;]*rename\s+column/i.test(t)},{id:"migration.rename-table",severity:"medium",title:"RENAME TABLE",description:"Renaming a table requires every dependent (views, functions, application code, ORMs) to know about the rename simultaneously. Add a view at the old name during transition.",test:t=>/alter\s+table\s+[^;]*rename\s+to\b/i.test(t)},{id:"migration.disable-rls",severity:"critical",title:"DISABLE ROW LEVEL SECURITY",description:"ALTER TABLE ... DISABLE ROW LEVEL SECURITY removes tenant isolation. If this is intentional, document it; otherwise this is a backdoor.",test:t=>/disable\s+row\s+level\s+security/i.test(t)},{id:"migration.truncate",severity:"high",title:"TRUNCATE",description:"TRUNCATE bypasses triggers (unless explicitly cascaded) and audit logs that depend on row-level events. Confirm intent.",test:t=>/^truncate\b/i.test(t)},{id:"migration.set-not-null-on-existing",severity:"medium",title:"ALTER COLUMN SET NOT NULL on existing column",description:"SET NOT NULL scans the entire table to validate. On large tables this is a long lock. Use NOT VALID + VALIDATE in two steps when possible.",test:t=>/alter\s+table\s+[^;]*alter\s+column\s+[^;]*set\s+not\s+null/i.test(t)}],b=/^(alter\s+table|create\s+(unique\s+)?index|drop\s+|truncate\b)/i,v=/set\s+lock_timeout/i,C=/--\s*jimmy:ignore(-file)?(?:\s+(\S+))?/i;function T(t){let e=[],n=t.split(`
`);for(let i=0;i<n.length;i++){let r=n[i].match(C);r&&e.push({line:i+1,file:r[1]==="-file",rule:r[2]??null})}return e}function L(t){let e=t.trim();return e===""||e.startsWith("--")}function S(t,e,n){if(e.some(r=>r.file))return!0;let i=t.location.line??0;if(i===0)return!1;for(let r of e)if(!(r.rule!==null&&r.rule!==t.ruleId)){if(r.line===i)return!0;if(r.line<i){let a=!0;for(let s=i-1;s>r.line;s--)if(!L(n[s-1]??"")){a=!1;break}if(a)return!0}}return!1}function w(t,e){let n=[],i=y(t),r=T(t),a=t.split(`
`),s=!1,d=!1,g=1;for(let u=0;u<i.length;u++){let{text:c,line:l}=i[u];b.test(c)&&!/temp(orary)?/i.test(c)&&(s||(g=l),s=!0),v.test(c)&&(d=!0);for(let o of E)if(o.test(c)){let p=`${e}:${o.id}:${u}`;n.push({id:m("migrations",o.id,p),category:"migrations",ruleId:o.id,severity:o.severity,title:`${o.title} (${f(e)})`,description:o.description,location:{file:e,line:l},evidence:{statement:c.slice(0,400)},remediation:o.remediation?o.remediation(c):void 0})}}return s&&!d&&n.push({id:m("migrations","migration.lock-timeout-missing",e),category:"migrations",ruleId:"migration.lock-timeout-missing",severity:"low",title:`Migration runs DDL without setting lock_timeout (${f(e)})`,description:"This migration runs DDL but never sets lock_timeout. Without it, a single long-running query can make the migration block all access and turn into an outage. Add `SET lock_timeout = '5s';` at the top.",location:{file:e,line:g},remediation:"SET lock_timeout = '5s';"}),r.length===0?n:n.filter(u=>!S(u,r,a))}export{w as lintSqlText};
